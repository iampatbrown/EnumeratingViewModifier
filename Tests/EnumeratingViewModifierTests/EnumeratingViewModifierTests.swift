import XCTest
@testable import EnumeratingViewModifier

final class EnumeratingViewModifierTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(EnumeratingViewModifier().text, "Hello, World!")
    }
}
